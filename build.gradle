plugins {
  id "org.openbakery.xcode-plugin" version "0.13.1"
}

xcodebuild {
	scheme = 'DemoApp'
  target = 'DemoApp'
	destination = [ 'iPhone 5s', 'iPad 2' ]
}

coverage {
	outputFormat = 'html'
	include = 'Core.*'
}